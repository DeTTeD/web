server {
    listen 80 default_server;
    location ^~ /hello/ {
        http://0.0.0.0:80;
    }
    location ^~ /uploads/ {
        root /home/box/web/;
    }
    location ~* ^.+\.\w+$ {
        root /home/box/web/public;
    }
    location / {
       return 404;
    }
}
